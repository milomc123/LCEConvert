{
  "name": "lceconvert-web",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "build:wasm": "node scripts/build_wasm.mjs",
    "build:wasm:test": "WASM_ENVIRONMENT=web,node node scripts/build_wasm.mjs",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview",
    "test": "npm run build:wasm:test && vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.2.0",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
